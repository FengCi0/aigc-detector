# Dataset Layout

Put training files in this structure:

- `data/dataset/ai/*.txt`
- `data/dataset/human/*.txt`

Rules:

- UTF-8 text files
- One sample per file
- Recommended minimum length: 50 Chinese characters
- Keep `ai` and `human` sample counts roughly balanced
